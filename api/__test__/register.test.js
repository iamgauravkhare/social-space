import { baseUrl } from "./config/api";

describe("register", () => {
  it("register user", async () => {
    // const result = await baseUrl.post("/register", {
    //   username: "rakesh",
    //   password: "rekesh",
    // });
    // expect(result).toBeTruthy();
    // expect(result.status).toBe(200);
    // expect(result.data.username).toBe("rakesh");
  });
});
